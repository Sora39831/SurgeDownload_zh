# Chinese (zh-CN) Localization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add native Simplified Chinese (zh-CN) support to Surge's TUI, CLI, settings, and browser extension using a T()-based translation system with English-text-as-key.

**Architecture:** A zero-dependency `internal/i18n` package provides `T(key)` which looks up the English key in an embedded `zh-CN.json` dictionary. If no translation exists, `T()` returns the key itself (readable English). `config.GetSettingsMetadata()` transparently applies translations to all setting labels/descriptions. The browser extension uses an isomorphic `t()` function with its own `zh-CN.json`. Language is set via `Settings.General.Language` (Go) or `navigator.language` / settings (extension), stored independently.

**Tech Stack:** Go 1.25 (`//go:embed`, stdlib only), SolidJS (extension), TypeScript

**Merge strategy:** Every user-facing English string gets wrapped with `T()` but the English text stays in source code — no abstract key names, no en.json baseline. Upstream changes to English text produce auto-mergeable diffs; missing dictionary entries silently fall back to English.

---

## File Structure

**New files (Go):**
- `internal/i18n/i18n.go` — `T()`, `Init()`, `SetLanguage()`, JSON loading with `//go:embed`
- `internal/i18n/i18n_test.go` — unit tests for T()
- `internal/i18n/zh-CN.json` — Chinese translation dictionary

**New files (Extension):**
- `extension/lib/i18n/index.ts` — `t()`, `initLanguage()`, language detection
- `extension/lib/i18n/zh-CN.json` — Extension Chinese translations

**Modified files (Go):**
- `internal/config/settings.go` — add `Language` field + wire T() in GetSettingsMetadata()
- `cmd/root_startup.go` — call `i18n.Init()` after config load
- `internal/tui/components/status.go` — Translate status labels
- `internal/tui/view.go` — Translate modal titles, messages, hints
- `internal/tui/view_settings.go` — Translate settings pane, tabs, help, category labels
- `internal/tui/view_category.go` — Translate category manager UI strings
- `internal/tui/view_dashboard_header.go` — Translate header labels
- `internal/tui/view_dashboard_list.go` — Translate search hints, tab labels
- `internal/tui/view_dashboard_details.go` — Translate detail pane labels
- `internal/tui/view_dashboard_graph.go` — Translate graph labels
- `internal/tui/view_dashboard_log.go` — Translate log pane labels
- `internal/tui/view_dashboard_chunkmap.go` — Translate chunk map labels (if any)
- `internal/tui/update_modals.go` — Translate modal button/confirm strings
- `internal/tui/update_settings.go` — Translate settings change feedback strings
- `internal/tui/update_dashboard.go` — Translate dashboard event strings (if any)
- `internal/tui/keys.go` — Translate key binding help text at render time
- `internal/tui/config.go` — Maybe (check for user-facing strings)
- `internal/tui/constants.go` — Maybe (check for user-facing strings)
- `internal/tui/helpers.go` — Maybe (check for user-facing strings)
- `cmd/root.go` — Translate root command description
- `cmd/add.go` — Translate add command description + output
- `cmd/ls.go` — Translate ls output
- `cmd/rm.go` — Translate rm output
- `cmd/pause.go` — Translate pause output
- `cmd/resume.go` — Translate resume output
- `cmd/refresh.go` — Translate refresh output
- `cmd/server.go` — Translate server command description
- `cmd/shutdown.go` — Translate shutdown output
- `cmd/connect.go` — Translate connect command description
- `cmd/root_headless.go` — Translate headless event output
- `cmd/bugreport.go` — Translate bug report output

**Modified files (Extension):**
- `extension/entrypoints/popup/App.tsx` — Load i18n on mount, pass t() to children
- `extension/entrypoints/popup/components/DownloadList.tsx` — Translate empty states
- `extension/entrypoints/popup/components/DownloadItem.tsx` — Translate status labels, action titles
- `extension/entrypoints/popup/components/SettingsView.tsx` — Translate all settings labels
- `extension/entrypoints/popup/components/StatusBadge.tsx` — Translate status text
- `extension/entrypoints/popup/components/DuplicateModal.tsx` — Translate modal text
- `extension/entrypoints/popup/components/ViewSwitch.tsx` — Translate tab labels
- `extension/lib/storage.ts` — Add LANGUAGE storage key

---

### Task 1: Create `internal/i18n/i18n.go` — Core translation engine

**Files:**
- Create: `internal/i18n/i18n.go`
- Test: `internal/i18n/i18n_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/i18n/i18n_test.go`:

```go
package i18n

import (
	"testing"
)

func TestT_ReturnsKeyWhenNoTranslation(t *testing.T) {
	// When no language is loaded, T() should return the key itself
	result := T("Hello World")
	if result != "Hello World" {
		t.Errorf("expected 'Hello World', got '%s'", result)
	}
}

func TestT_ReturnsTranslationWhenLoaded(t *testing.T) {
	// Load zh-CN and verify translation
	err := Init("zh-CN")
	if err != nil {
		t.Fatalf("Init failed: %v", err)
	}
	result := T("Queued")
	if result != "排队中" {
		t.Errorf("expected '排队中', got '%s'", result)
	}
}

func TestT_FallbackWhenKeyMissing(t *testing.T) {
	err := Init("zh-CN")
	if err != nil {
		t.Fatalf("Init failed: %v", err)
	}
	// This key doesn't exist in zh-CN.json
	result := T("Some nonexistent string")
	if result != "Some nonexistent string" {
		t.Errorf("expected fallback to key, got '%s'", result)
	}
}

func TestT_EnglishModeReturnsKey(t *testing.T) {
	err := Init("en")
	if err != nil {
		t.Fatalf("Init failed: %v", err)
	}
	result := T("Queued")
	if result != "Queued" {
		t.Errorf("expected 'Queued', got '%s'", result)
	}
}

func TestSetLanguage(t *testing.T) {
	err := SetLanguage("zh-CN")
	if err != nil {
		t.Fatalf("SetLanguage failed: %v", err)
	}
	result := T("Queued")
	if result != "排队中" {
		t.Errorf("expected '排队中', got '%s'", result)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/i18n/ -v -count=1`
Expected: Compile error — package doesn't exist yet

- [ ] **Step 3: Create a minimal zh-CN.json for tests**

Create `internal/i18n/zh-CN.json` with just the test-referenced translations:

```json
{
  "Queued": "排队中",
  "Hello World": "你好世界"
}
```

(Full translation dictionary will be expanded later.)

- [ ] **Step 4: Write the implementation**

Create `internal/i18n/i18n.go`:

```go
package i18n

import (
	"embed"
	"encoding/json"
	"sync"
)

//go:embed *.json
var localeFS embed.FS

var (
	mu       sync.RWMutex
	messages map[string]string // current language translations
)

// Init loads translations for the given language code.
// lang should be "en", "zh-CN", etc.
// For "en", no file is loaded — T() returns keys as-is.
func Init(lang string) error {
	if lang == "" || lang == "en" {
		mu.Lock()
		messages = nil
		mu.Unlock()
		return nil
	}

	data, err := localeFS.ReadFile(lang + ".json")
	if err != nil {
		// If file doesn't exist, fall back to identity map (English)
		mu.Lock()
		messages = nil
		mu.Unlock()
		return nil
	}

	var dict map[string]string
	if err := json.Unmarshal(data, &dict); err != nil {
		return err
	}

	mu.Lock()
	messages = dict
	mu.Unlock()
	return nil
}

// SetLanguage is a convenience wrapper for Init, useful for hot-reload in tests.
func SetLanguage(lang string) error {
	return Init(lang)
}

// T translates key to the current language, or returns key itself if no translation found.
func T(key string) string {
	mu.RLock()
	dict := messages
	mu.RUnlock()

	if dict == nil {
		return key
	}
	if v, ok := dict[key]; ok {
		return v
	}
	return key
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `go test ./internal/i18n/ -v -count=1`
Expected: All 5 tests pass

- [ ] **Step 6: Commit**

```bash
git add internal/i18n/
git commit -m "feat(i18n): add core translation engine with T() function and zh-CN.json"
```

---

### Task 2: Expand zh-CN.json with all translations

**Files:**
- Create: `internal/i18n/zh-CN.json`

- [ ] **Step 1: Replace minimal zh-CN.json with the full translation dictionary**

This step covers ALL strings. We'll create a single comprehensive JSON file now so subsequent tasks can reference it. The translations are written by you based on your knowledge of the project.

Create `internal/i18n/zh-CN.json`:

```json
{
  "Queued": "排队中",
  "Downloading": "下载中",
  "Paused": "已暂停",
  "Completed": "已完成",
  "Error": "出错",
  "Unknown": "未知",
  "Loading...": "加载中...",
  "Terminal too small (min: %d×%d)": "终端窗口太小（最小：%d×%d）",
  "Shutting Down": "正在关闭",
  "Pausing downloads and saving resume state...": "正在暂停下载并保存续传状态...",
  "Please wait": "请稍候",
  "Add Download": "添加下载",
  "URL:": "URL：",
  "Mirrors:": "镜像：",
  "Path:": "路径：",
  "Filename:": "文件名：",
  " Select Directory ": " 选择目录 ",
  " Select URL File (.txt) ": " 选择 URL 文件 (.txt) ",
  "Extension Download": "扩展下载",
  "Duplicate Detected": "检测到重复",
  "A download with this URL already exists": "该 URL 的下载已存在",
  "Batch Import": "批量导入",
  "Add %d downloads?": "添加 %d 个下载？",
  "Bug Report": "问题反馈",
  "What would you like to report?": "您想要报告什么？",
  "1) Surge Core (CLI/TUI/server)\n2) Browser Extension": "1) Surge 核心（CLI/TUI/服务端）\n2) 浏览器扩展",
  "Core Bug Report": "核心问题反馈",
  "Include system details in issue body?": "在 issue 中包含系统详情？",
  "(OS, version, commit)": "（操作系统、版本、提交号）",
  "Include latest debug log path in issue body?": "在 issue 中包含最新的调试日志路径？",
  "Choose yes to prefill the latest path when available.": "选择是将在可用时预填最新路径。",
  "Keyboard Shortcuts": "键盘快捷键",
  "Refresh URL": "刷新 URL",
  "New URL:": "新 URL：",
  "Update Available": "有可用更新",
  "A new version of Surge is available: %s": "Surge 新版本可用：%s",
  "Current: %s": "当前版本：%s",
  "Settings": "设置",
  "Terminal too small for settings view": "终端窗口太小，无法显示设置界面",
  "No settings categories available": "没有可用的设置分类",
  "No settings available in this category": "此分类中没有可用设置",
  "No setting selected": "未选择设置",
  "General": "通用",
  "Network": "网络",
  "Performance": "性能",
  "Categories": "分类",
  "Extension": "扩展",
  "Gen": "通用",
  "Net": "网络",
  "Perf": "性能",
  "Cats": "分类",
  "Ext": "扩展",
  "esc: save/close  tab: next tab  enter: edit": "esc: 保存/关闭  tab: 下个标签  enter: 编辑",
  "esc close | enter edit": "esc 关闭 | enter 编辑",
  "Category Manager": "分类管理",
  "Terminal too small for category manager": "终端窗口太小，无法显示分类管理界面",
  "Auto-Sort Downloads: ": "自动分类下载：",
  "Auto-Sort: ": "自动分类：",
  "  (t to toggle)": "  （按 t 切换）",
  "  (t)": "  （t）",
  "categories": "个分类",
  "esc: save/close  enter: edit/save  del: remove": "esc: 保存/关闭  enter: 编辑/保存  del: 删除",
  "esc close | enter edit | del rm": "esc 关闭 | enter 编辑 | del 删除",
  "(Unnamed Category)": "（未命名分类）",
  "+ Add Category": "+ 添加分类",
  "Press Enter to create a new category\nor press 'a' to add.": "按 Enter 创建新分类\n或按 'a' 添加。",
  "Name:": "名称：",
  "Description:": "描述：",
  "Pattern (Regex):": "规则（正则表达式）：",
  "Pattern:": "规则：",
  "Path:": "路径：",
  "tab: next field  enter: save  esc: cancel": "tab: 下一个字段  enter: 保存  esc: 取消",
  " Connected to ": " 已连接到 ",
  " Serving at ": " 服务运行于 ",
  " Server ": " 服务 ",
  "No downloads found.": "未找到下载。",
  "Queued": "排队中",
  "Active": "活动中",
  "Done": "已完成",
  " [esc exit]": " [esc 退出]",
  " [f to clear]": " [f 清除]",
  "No download selected": "未选择下载",
  "Downloading": "下载中",
  "Pausing...": "暂停中...",
  "Resuming...": "恢复中...",
  "Size:": "大小：",
  "Speed:": "速度：",
  "Conns:": "连接：",
  "Time:": "用时：",
  "ETA:": "预计剩余：",
  "File: ": "文件：",
  "Path: ": "路径：",
  "ID:   ": "ID：   ",
  "Mirrors": "镜像",
  "Active / Total (%d Errors)": "活跃 / 总计（%d 个错误）",
  "Progress: ": "进度：",
  " MB/s (Avg)": " MB/s（平均）",
  "Done": "已完成",
  "Paused": "已暂停",
  "Resume": "恢复",
  "Pause": "暂停",
  "Cancel": "取消",
  "(Not generated yet)": "（尚未生成）",
  "Copied!": "已复制！",
  " [Enter to Copy]": " [按 Enter 复制]",
  "Open [Enter]": "打开 [Enter]",
  "Value: ": "值：",
  "[Tab] Browse: ": "[Tab] 浏览：",
  "Action: ": "操作：",
  "(default)": "（默认）",
  "True": "是",
  "False": "否",
  " (Ignored)": "（已忽略）",
  " Requires Restart": " 需要重启",
  "Settings saved!": "设置已保存！",
  "Restart now to take effect?": "立即重启生效？",
  "Quit Surge": "退出 Surge",
  "Are you sure you want to quit?": "确定要退出吗？",
  "%d active download(s) will be paused": "%d 个正在下载的任务将被暂停",
  "Y": "是",
  "N": "否",
  "Yes": "是",
  "No": "否",
  " Reset all categories to defaults?": "将所有分类重置为默认值？",
  " This will overwrite your custom rules.": "这将覆盖您的自定义规则。",
  " Category Reset ": " 分类重置 ",
  " Restart Required ": " 需要重启 ",
  "Open file": "打开文件",
  "Open folder": "打开文件夹",

  "Successfully added %d downloads.": "成功添加 %d 个下载。",
  "Error adding %s: %v": "添加 %s 出错：%v",
  "failed to add any downloads": "未能添加任何下载",
  "no valid URLs to add": "没有有效的 URL",
  "Removed %d completed downloads.": "已删除 %d 个已完成的下载。",
  "Successfully updated URL for download %s": "成功更新下载 %s 的 URL",
  "Pausing all downloads is not yet implemented for running server.": "暂停所有下载功能在运行服务器模式下尚未实现。",
  "Resuming all downloads is not yet implemented for running server.": "恢复所有下载功能在运行服务器模式下尚未实现。",

  "Started: %s [%s]": "已开始：%s [%s]",
  "Completed: %s [%s] (in %s)": "已完成：%s [%s]（用时 %s）",
  "Error: %s [%s]: %v": "出错：%s [%s]：%v",
  "Queued: %s [%s]": "已排队：%s [%s]",
  "Paused: %s [%s]": "已暂停：%s [%s]",
  "Resumed: %s [%s]": "已恢复：%s [%s]",
  "Removed: %s [%s]": "已删除：%s [%s]",

  "Blazing fast TUI download manager built in Go for power users": "使用 Go 语言构建的极速 TUI 下载管理器",
  "Surge is a blazing fast TUI download manager built in Go for power users. Find more info here: https://github.com/SurgeDM/Surge": "Surge 是一款使用 Go 语言构建的极速 TUI 下载管理器，专为高级用户打造。更多信息：https://github.com/SurgeDM/Surge",
  "Add a new download to the running Surge instance": "向正在运行的 Surge 实例添加新下载",
  "Add one or more URLs to the download queue of a running Surge instance.": "将一个或多个 URL 添加到正在运行的 Surge 实例的下载队列。",
  "Server host to connect/control (or set SURGE_HOST), e.g. 127.0.0.1:1700": "要连接/控制的服务器主机（或设置 SURGE_HOST 环境变量），例如 127.0.0.1:1700",
  "Bearer token (or set SURGE_TOKEN)": "Bearer 令牌（或设置 SURGE_TOKEN 环境变量）",
  "Allow plain HTTP for non-loopback remote targets": "允许对非回环远程目标使用明文 HTTP",
  "Skip TLS certificate verification for remote targets": "跳过远程目标的 TLS 证书验证",
  "PEM bundle to trust for remote HTTPS targets": "用于信任远程 HTTPS 目标的 PEM 证书包",
  "File containing URLs to download (one per line)": "包含下载 URL 的文件（每行一个）",
  "Port to listen on (default: 8080 or first available)": "监听端口（默认：8080 或第一个可用端口）",
  "Output directory (defaults to current working directory)": "输出目录（默认为当前工作目录）",
  "Do not auto-resume paused downloads on startup": "启动时不自动恢复已暂停的下载",
  "Exit when all downloads complete": "所有下载完成后退出",

  "MB": "MB",
  "KB": "KB",
  " conns": " 个连接",
  " retries": " 次重试",
  " seconds": " 秒",
  " (0.0-1.0)": "（0.0-1.0）"
}
```

- [ ] **Step 2: Verify JSON is valid**

Run: `python3 -c "import json; json.load(open('internal/i18n/zh-CN.json'))" ; echo "OK"`
Expected: "OK"

- [ ] **Step 3: Commit**

```bash
git add internal/i18n/zh-CN.json
git commit -m "feat(i18n): add zh-CN translation dictionary"
```

---

### Task 3: Add `Language` field to config and wire i18n into startup

**Files:**
- Modify: `internal/config/settings.go`
- Modify: `cmd/root_startup.go`

- [ ] **Step 1: Add Language field to GeneralSettings**

In `internal/config/settings.go`, add the Language field:

```go
// GeneralSettings contains application behavior settings.
type GeneralSettings struct {
	// ... existing fields ...
	Language string `json:"language" ui_label:"Language" ui_desc:"Interface language (en, zh-CN). Restart required." ui_restart:"true"`
	// ... rest of existing fields ...
}
```

Add it after `LiveSpeedGraph` and before `LogRetentionCount` (or another logical position).

Also add it to the reset function in `view_settings.go`:

```go
case "language":
    m.Settings.General.Language = defaults.General.Language
```

- [ ] **Step 2: Wire i18n.Init() into startup**

In `cmd/root_startup.go`, find the function that loads settings (likely `getSettings()` or `initializeGlobalState()`) and add:

```go
import "github.com/SurgeDM/Surge/internal/i18n"

// Inside initializeGlobalState():
if err := i18n.Init(settings.General.Language); err != nil {
    utils.Debug("i18n init failed: %v", err)
}
```

Read the file first to find the exact insertion point.

- [ ] **Step 3: Build and verify no compile errors**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add internal/config/settings.go cmd/root_startup.go
git commit -m "feat(i18n): add Language setting and wire i18n init into startup"
```

---

### Task 4: Apply T() in `GetSettingsMetadata()` for setting labels/descriptions

**Files:**
- Modify: `internal/config/settings.go`

- [ ] **Step 1: Import i18n and wrap labels/descriptions**

In `GetSettingsMetadata()`, import `i18n` and wrap the label and description:

```go
import "github.com/SurgeDM/Surge/internal/i18n"

// After reading label and desc from struct tags:
label = i18n.T(label)
desc = i18n.T(desc)

// In the SettingMeta constructor:
catMetas = append(catMetas, SettingMeta{
    Key:             key,
    Label:           i18n.T(label),   // already applied above
    Description:     i18n.T(desc),    // already applied above
    Type:            typStr,
    RequiresRestart: settingField.Tag.Get("ui_restart") == "true",
})
```

Note: The `catLabel` variable (used as map key) must NOT be translated — it's used for lookups.

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/config/settings.go
git commit -m "feat(i18n): translate setting labels and descriptions via GetSettingsMetadata()"
```

---

### Task 5: Translate status labels in `components/status.go`

**Files:**
- Modify: `internal/tui/components/status.go`

- [ ] **Step 1: Add i18n import and wrap Label() method**

Import `i18n` and modify `Label()`:

```go
import "github.com/SurgeDM/Surge/internal/i18n"

func (s DownloadStatus) Label() string {
    if info, ok := statusMap[s]; ok {
        return i18n.T(info.label)
    }
    return "Unknown"
}
```

Also wrap the fallback "Unknown" in `Label()` and `Render()`:

```go
return i18n.T("Unknown")
```

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/components/status.go
git commit -m "feat(i18n): translate download status labels"
```

---

### Task 6: Translate `internal/tui/view.go` — all modal and dashboard strings

**Files:**
- Modify: `internal/tui/view.go`

- [ ] **Step 1: Read the file and wrap ALL user-facing strings with T()**

Scan for every string literal that appears in the UI (modal titles, messages, labels, hints, confirmation dialogs). Every occurrence where the user sees English text should be wrapped.

Specific locations to modify:
- Line 69: `"Loading..."` → `i18n.T("Loading...")`
- Line 74: `"Terminal too small (min: %d×%d)"` → `i18n.T(...)` (note: format string is passed to Sprintf ahead, wrap the format)
- Line 79-83: `"Shutting Down"`, `"Pausing downloads and saving resume state..."`, `"Please wait"`
- Line 98-99: `"Add Download"`, `"URL:"`, `"Mirrors:"`, `"Path:"`, `"Filename:"`
- Line 118: `" Select Directory "`
- Line 143-145: `"Duplicate Detected"`, `"A download with this URL already exists"`
- Line 175-178: `"Extension Download"`, `"Path:"`, `"Filename:"`
- Line 198: `" Select URL File (.txt) "`
- Line 218-220: `"Batch Import"`, `"Add %d downloads?"`
- Line 238-243: `"Bug Report"`, `"What would you like to report?"`, etc.
- Line 252-256: `"Core Bug Report"`, `"Include system details in issue body?"`, etc.
- Line 270-275: `"Core Bug Report"`, `"Include latest debug log path in issue body?"`, etc.
- Line 300-304: `"Update Available"`, `"A new version of Surge is available: %s"`, `"Current: %s"`
- Line 319-320: `"Refresh URL"`, `"New URL:"`
- Line 342: `"Keyboard Shortcuts"`
- Line 399: `"No download selected"`
- And all other user-facing strings in the file

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/view.go
git commit -m "feat(i18n): translate TUI view.go strings"
```

---

### Task 7: Translate `internal/tui/view_settings.go`

**Files:**
- Modify: `internal/tui/view_settings.go`

- [ ] **Step 1: Wrap all user-facing strings**

Locations:
- Line 29: `"Terminal too small for settings view"`
- Line 30 etc.: `" Settings "` pane title (all occurrences)
- Line 38: `"No settings categories available"`
- Line 58: `"No settings available in this category"`
- Line 82: Error prefix `"✖ "` + `m.settingsError` — wrap the prefix only
- Line 129-139: Category labels `"General"` → `"Network"` etc. in `shortSettingsCategoryLabel()`
- Line 189: `"esc: save/close  tab: next tab  enter: edit"`
- Line 191: `"esc close | enter edit"`
- Line 234: `"(No settings)"`
- Line 302: `"No setting selected"`
- Line 316: `"(Not generated yet)"`
- Line 321: `"Copied!"`
- Line 325: `" [Enter to Copy]"`
- Line 331: `"Open [Enter]"`
- Line 333: `" (Ignored)"`
- Line 339: `"Value: "`
- Line 342: `"[Tab] Browse: "`
- Line 344: `"Action: "`
- Line 367: `"↺ Requires Restart"`
- Line 769-779: Unit strings in `getSettingUnit()` — `" MB"`, `" KB"`, `" conns"`, `" retries"`, `" seconds"`, `" (0.0-1.0)"`
- Line 808-813: Theme values — `"< System >"`, `"< Light >"`, `"< Dark >"`
- Line 830-833: Bool values — `"True"`, `"False"`
- Line 857: `"(default)"`

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/view_settings.go
git commit -m "feat(i18n): translate settings view strings"
```

---

### Task 8: Translate `internal/tui/view_category.go` — category manager

**Files:**
- Modify: `internal/tui/view_category.go`

- [ ] **Step 1: Wrap all user-facing strings**

- Line 24: `"Terminal too small for category manager"`
- Line 25: `" Category Manager "` pane title
- Line 59-66: `"Auto-Sort Downloads: "`, `"  (t to toggle)"`, `"Auto-Sort: "`, `"  (t)"`
- Line 69: `"%d categories"` — format string
- Line 125-129: Help text strings
- Line 182: `"(Unnamed Category)"`
- Line 200: `"+ Add Category"`
- Line 215: Multi-line hint about creating/add category
- Line 232: `"Name:"`, `"Description:"`, etc. in detail view
- Line 255: `"Name:"`, `"Description:"`, `"Pattern:"`, `"Path:"` in edit view
- Line 275: `"tab: next field  enter: save  esc: cancel"`

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/view_category.go
git commit -m "feat(i18n): translate category manager strings"
```

---

### Task 9: Translate dashboard view files

**Files:**
- Modify: `internal/tui/view_dashboard_header.go`
- Modify: `internal/tui/view_dashboard_list.go`
- Modify: `internal/tui/view_dashboard_details.go`
- Modify: `internal/tui/view_dashboard_graph.go`
- Modify: `internal/tui/view_dashboard_log.go`
- Modify: `internal/tui/view_dashboard_chunkmap.go`

- [ ] **Step 1: Read each file and wrap user-facing strings**

**view_dashboard_header.go:**
- Line 47: `" Connected to "`
- Line 49: `" Serving at "`
- Line 82: `" Server "` pane title

**view_dashboard_list.go:**
- Line 35: `" [esc exit]"`  
- Line 39: `" [f to clear]"`
- Tab labels from `renderTabs()` — these are "Queued", "Active", "Done" in tabs.go

**view_dashboard_details.go:**
- Labels: `"URL: "`, `"File: "`, `"Path: "`, `"ID:   "`
- Stats labels: `"Size:"`, `"Speed:"`, `"Conns:"`, `"Time:"`, `"ETA:"`
- Mirror display: `"Mirrors"`, `"%d Active / %d Total (%d Errors)"`
- Error display: `"Error: "`
- Speed/status strings: `"Paused"`, `"Resuming..."`, `"Pausing..."`, `"Done"`, etc.
- `"N/A"`

**view_dashboard_graph.go:**
- Check for axis labels or title strings

**view_dashboard_log.go:**
- Check for column headers

**view_dashboard_chunkmap.go:**
- Check for labels

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/view_dashboard_header.go internal/tui/view_dashboard_list.go internal/tui/view_dashboard_details.go internal/tui/view_dashboard_graph.go internal/tui/view_dashboard_log.go internal/tui/view_dashboard_chunkmap.go
git commit -m "feat(i18n): translate dashboard view strings"
```

---

### Task 10: Translate update files (`update_*.go`, `keys.go`, `helpers.go`)

**Files:**
- Modify: `internal/tui/update_modals.go`
- Modify: `internal/tui/update_settings.go`
- Modify: `internal/tui/update_dashboard.go`
- Modify: `internal/tui/keys.go`
- Modify: `internal/tui/helpers.go`

- [ ] **Step 1: Read each file and wrap strings**

**update_modals.go:** Look for any confirmation/button text, error messages.

**update_settings.go:** Look for success/error messages when saving settings.

**update_dashboard.go:** Look for event-based status text.

**keys.go:** Key binding `Help` text — these need T() at render time or help view time, not at init time since i18n isn't loaded yet at init.

**helpers.go:** Check for any user-facing strings.

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add internal/tui/update_modals.go internal/tui/update_settings.go internal/tui/update_dashboard.go internal/tui/keys.go internal/tui/helpers.go
git commit -m "feat(i18n): translate update handlers and key binding help"
```

---

### Task 11: Translate CLI command descriptions

**Files:**
- Modify: `cmd/root.go`
- Modify: `cmd/add.go`
- Modify: `cmd/server.go`
- Modify: `cmd/connect.go`

- [ ] **Step 1: Read and modify each command file**

For each `cobra.Command`, wrap `Short` and `Long` strings with `T()`. Also wrap flag description strings.

**cmd/root.go:**
- `Short: "Blazing fast TUI download manager built in Go for power users"`
- `Long: "Surge is a..."` string
- Flag descriptions for `--host`, `--token`, `--batch`, `--port`, `--output`, `--no-resume`, `--exit-when-done`, etc.

**cmd/add.go:**
- `Short: "Add a new download to the running Surge instance"`
- `Long: "Add one or more URLs..."`

**cmd/server.go:**
- `Short` and `Long`

**cmd/connect.go:**
- `Short` and `Long`

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add cmd/root.go cmd/add.go cmd/server.go cmd/connect.go
git commit -m "feat(i18n): translate CLI command descriptions"
```

---

### Task 12: Translate CLI output messages

**Files:**
- Modify: `cmd/add.go`
- Modify: `cmd/ls.go`
- Modify: `cmd/rm.go`
- Modify: `cmd/pause.go`
- Modify: `cmd/resume.go`
- Modify: `cmd/refresh.go`
- Modify: `cmd/shutdown.go`
- Modify: `cmd/root_headless.go`
- Modify: `cmd/bugreport.go`

- [ ] **Step 1: Read and modify each file**

For each call to `fmt.Printf`, `fmt.Println`, `fmt.Fprintf` that outputs user-facing text, wrap the format string with T().

Pattern:
```go
// Before:
fmt.Printf("Successfully added %d downloads.\n", count)
// After:
fmt.Printf(i18n.T("Successfully added %d downloads.\n"), count)
```

For Println (no formatting):
```go
// Before:
fmt.Println("No downloads found.")
// After:
fmt.Println(i18n.T("No downloads found."))
```

For `fmt.Fprintf`:
```go
// Before:
fmt.Fprintf(out, "%s\n\n%s\n", message, reportURL)
// After:
fmt.Fprintf(out, "%s\n\n%s\n", message, reportURL) // The format itself is not user-facing, but the message might be
// Actually, for bugreport.go the format structure is not a translatable string — just the message variable.
```

Note: Only the format string (first arg after writer) should be wrapped, not the data arguments.

- [ ] **Step 2: Build and verify**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add cmd/add.go cmd/ls.go cmd/rm.go cmd/pause.go cmd/resume.go cmd/refresh.go cmd/shutdown.go cmd/root_headless.go cmd/bugreport.go
git commit -m "feat(i18n): translate CLI output messages"
```

---

### Task 13: Create extension i18n system and translate extensions

**Files:**
- Create: `extension/lib/i18n/index.ts`
- Create: `extension/lib/i18n/zh-CN.json`
- Modify: `extension/lib/storage.ts` — add LANGUAGE key
- Modify: `extension/entrypoints/popup/App.tsx` — load i18n on mount
- Modify: `extension/entrypoints/popup/components/DownloadList.tsx`
- Modify: `extension/entrypoints/popup/components/DownloadItem.tsx`
- Modify: `extension/entrypoints/popup/components/SettingsView.tsx`
- Modify: `extension/entrypoints/popup/components/StatusBadge.tsx`
- Modify: `extension/entrypoints/popup/components/DuplicateModal.tsx`
- Modify: `extension/entrypoints/popup/components/ViewSwitch.tsx`

- [ ] **Step 1: Create extension i18n module**

`extension/lib/i18n/index.ts`:

```typescript
import zhCN from './zh-CN.json';

type Dict = Record<string, string>;

const locales: Record<string, Dict> = {
  'zh-CN': zhCN,
};

let currentDict: Dict | null = null;

export function t(key: string): string {
  if (currentDict && currentDict[key]) {
    return currentDict[key];
  }
  return key;
}

export async function initLanguage(): Promise<string> {
  const stored = await browser.storage.local.get('language');
  const lang = (stored.language as string) || detectLanguage();
  await setLanguage(lang);
  return lang;
}

export async function setLanguage(lang: string): Promise<void> {
  currentDict = locales[lang] || null;
  await browser.storage.local.set({ language: lang });
}

function detectLanguage(): string {
  try {
    const lang = navigator.language;
    if (lang.startsWith('zh')) return 'zh-CN';
  } catch { /* ignore */ }
  return 'en';
}
```

`extension/lib/i18n/zh-CN.json`:

```json
{
  "Active": "活动中",
  "History": "历史记录",
  "Settings": "设置",
  "No active downloads": "没有活跃的下载",
  "Downloads will appear here automatically": "下载将自动显示在此处",
  "No history downloads": "没有历史下载记录",
  "Completed downloads will appear here": "已完成的下载将显示在此处",
  "Downloading": "下载中",
  "Paused": "已暂停",
  "Queued": "排队中",
  "Completed": "已完成",
  "Error": "出错",
  "Pause": "暂停",
  "Resume": "恢复",
  "Open folder": "打开文件夹",
  "Open file": "打开文件",
  "Cancel": "取消",
  "Duplicate Download": "重复下载",
  "This file is already being downloaded:": "此文件已在下载中：",
  "Skip": "跳过",
  "Download Anyway": "仍然下载",
  "Offline": "离线",
  "Invalid": "无效",
  "Connected": "已连接",
  "Intercept Downloads": "拦截下载",
  "Show Notifications": "显示通知",
  "Server": "服务器",
  "Server URL": "服务器地址",
  "Auth Token": "认证令牌",
  "Support": "支持",
  "Report a Bug": "报告问题",
  "Saving...": "保存中...",
  "Saved": "已保存",
  "Failed to save": "保存失败",
  "Removing...": "删除中...",
  "Removed": "已删除",
  "Failed to remove": "删除失败",
  "Save": "保存",
  "Delete": "删除",
  "Token can be obtained from ": "令牌可从 ",
  " > Settings > Extension": " > 设置 > 扩展",
  "Invalid Token": "无效的令牌",
  "Token is Required": "需要令牌",
  "Enter your token": "输入您的令牌",
  "Language": "语言",
  "简体中文": "简体中文",
  "English": "English"
}
```

- [ ] **Step 2: Add language support to extension storage**

In `extension/lib/storage.ts`, add:

```typescript
export const LANGUAGE_KEY = 'language';
```

- [ ] **Step 3: Wire i18n into extension App.tsx**

In `App.tsx`, import and call `initLanguage()` on mount, then pass `t` to components or make it a module-level import.

The simplest approach: export `t` from the i18n module and import it directly in each component. Since `t()` reads from a module-level variable that gets populated by `initLanguage()`, components that render after init will work correctly.

In `App.tsx`, add to `onMount`:
```typescript
import { initLanguage } from '../../lib/i18n';

// Inside onMount, after loadSettings():
await initLanguage();
// Force re-render by toggling a signal if needed
```

Since SolidJS doesn't re-render on module variable change, we need a reactive signal. Create a `localeSignal` in the i18n module and export `useLocale()`:

```typescript
// extension/lib/i18n/index.ts
import { createSignal } from 'solid-js';

const [locale, setLocale] = createSignal('en');

export function useLocale() {
  return locale;
}

export async function initLanguage(): Promise<string> {
  // ... detect language, load dict, then:
  setLocale(lang);
  return lang;
}
```

Components that use `t()` won't re-render automatically when the locale changes. The easiest fix: have each component import `useLocale()` and call it (even unused) so SolidJS tracks the dependency.

Actually, the cleanest approach for SolidJS is to make `t` a function that takes a key and is accessed through a context. Or simply: since `initLanguage()` is called in `onMount` and the first render happens after, it should be fine for initial render. For language switching at runtime, we'd need to handle re-renders.

Let's keep it simple: just export `t` as a plain function and call `initLanguage()` in `onMount`. Since the popup is re-created each time it opens (typical for browser extensions), the initial render will always use the detected language.

For runtime language switching within the same popup session (if you add a language selector to the extension settings), components would need to re-render. This can be handled by exporting a signal:

- [ ] **Step 4: Translate each extension component**

For each file, wrap user-facing JSX strings with `t()`:
```tsx
import { t } from '../../../lib/i18n';

// Usage:
<span>{t('Active')}</span>
```

- [ ] **Step 5: Build the extension and verify**

Run (from `extension/` directory):
```bash
npm run check
```
Expected: No TypeScript errors

- [ ] **Step 6: Commit**

```bash
git add extension/lib/i18n/ extension/lib/storage.ts extension/entrypoints/popup/App.tsx extension/entrypoints/popup/components/
git commit -m "feat(i18n): add extension i18n system and translate extension strings"
```

---

### Task 14: Full build and smoke test

**Files:**
- All modified files

- [ ] **Step 1: Full Go build**

Run: `go build ./...`
Expected: No errors

- [ ] **Step 2: Run all Go tests**

Run: `go test ./... 2>&1 | tail -20`
Expected: All tests pass (\no FAIL)

- [ ] **Step 3: Run extension type check**

Run (from `extension/`): `npm run check`
Expected: No errors

- [ ] **Step 4: Run extension tests**

Run (from `extension/`): `npm run test`
Expected: All tests pass

- [ ] **Step 5: Verify T() handles edge cases**

Run: `go test ./internal/i18n/ -v -count=1`
Expected: All tests pass

---

## Spec Coverage Check

| Spec Requirement | Task |
|---|---|
| T() function with English-as-key | Task 1 |
| zh-CN.json dictionary | Task 2 |
| Language field in settings | Task 3 |
| i18n.Init() at startup | Task 3 |
| GetSettingsMetadata() wraps labels | Task 4 |
| Status labels translation | Task 5 |
| TUI view.go strings | Task 6 |
| TUI settings view strings | Task 7 |
| TUI category manager strings | Task 8 |
| Dashboard view strings | Task 9 |
| Key bindings + update strings | Task 10 |
| CLI command descriptions | Task 11 |
| CLI output messages | Task 12 |
| Extension i18n system | Task 13 |
| Extension component translation | Task 13 |
| Build verification | Task 14 |
